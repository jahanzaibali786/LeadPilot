<!doctype html><html><head><meta name="viewport" content="width=device-width,initial-scale=1"><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"><link rel="stylesheet" href="{{ asset('css/app.css') }}"><title>Login · LeadPilot PK</title></head><body><div class="auth-page"><section class="auth-art"><span class="eyebrow text-white-50">AI website opportunity intelligence</span><h1>Find the businesses that need you most.</h1><p>Build compliant Google Places campaigns, qualify website opportunities and manage outreach in one focused Pakistani sales workspace.</p></section><section class="auth-form"><form class="auth-box" method="post" action="{{ route('login') }}">@csrf<h2 class="fw-bold">Welcome back</h2><p class="text-secondary mb-4">Sign in to your lead workspace.</p>@if(session('status'))<div class="alert alert-success">{{session('status')}}</div>@endif @error('email')<div class="alert alert-danger">{{ $message }}</div>@enderror<label class="form-label">Email address</label><input class="form-control mb-3" type="email" name="email" value="{{ old('email') }}" required autofocus><div class="d-flex justify-content-between"><label class="form-label">Password</label><a class="small" href="{{route('password.request')}}">Forgot password?</a></div><input class="form-control mb-3" type="password" name="password" required><label class="mb-4 small"><input type="checkbox" name="remember"> Remember me</label><button class="btn btn-brand w-100 py-2">Sign in</button><p class="text-center mt-4 small">New here? <a href="{{ route('register') }}">Create an account</a></p></form></section></div></body></html>
